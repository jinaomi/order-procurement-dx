# 2026-07-31

## Đã làm

Tiếp nối từ devlog 2026-07-26 (khi đó vẫn còn làm trong `case-management`/`order-platform-dx`):

1. **経営ダッシュボード AI comment**: chuyển từ tự động chạy mỗi lần vào trang sang **manual trigger** (nút "AI経営コメントを生成") — giảm chi phí API token không cần thiết.
2. **AI照合**: sửa bug double-counting tồn kho (đã ghi trong CLAUDE.md phần AI照合).
3. **受注検索**: thêm cột AI照合 hiện risk level xấu nhất của từng đơn.
4. **請求書管理**: thêm tìm kiếm theo 受注番号; sửa layout 発行日 (date range) — sau nhiều lần thử (chung hàng + width:100% tràn khung, ép px cứng thì tự cắt chữ input ngày), kết luận đúng: **field date-range cần đứng 1 hàng riêng full-width**, không nhét chung hàng với field khác trừ khi cột đủ rộng (như `CaseSearch.js` làm với cột nửa trang).
5. **UI accent màu cam** (`#b85a25`): áp cho icon/text liên quan AI (SmartToyIcon dashboard, AutoAwesomeIcon, footer text "受注管理システム" ở sidebar) — theo yêu cầu dựa trên artifact User Guide đã làm trước đó. Icon AIチャット trong sidebar menu **cố ý giữ màu teal gốc**, không đổi cam (theo yêu cầu riêng).
6. **SalesDashboard**: thêm biểu đồ `@mui/x-charts` (pie 受注ステータス, line 月別売上, bar 取引先別/商品別売上) — **giữ nguyên bảng thô cũ**, chart chỉ thêm bổ sung.
7. **Fix 2 bug quan trọng trong `AiOrderExtractionService.cs`** (phát hiện qua test thật với ảnh chụp tay):
   - **取引先名 bị rớt hoàn toàn** khi chữ viết tay không khớp chính xác DB (vd viết tắt `中央工業（株）` vs DB `中央工業株式会社`) — nguyên nhân: tool schema không có confidence riêng cho `customer_name`, không bắt buộc, system prompt khuyến khích bỏ trống khi không chắc. Fix: thêm `customer_name_confidence`, bắt buộc luôn trả nguyên văn + confidence, và **chuẩn hoá hậu tố pháp nhân** (`株式会社`/`（株）`/`(株)`/`㈱`/`有限会社`/...) khi so khớp DB thay vì so chuỗi chính xác tuyệt đối.
   - **単価 không tự điền** dù match được sản phẩm — nguyên nhân: code có `matchedProduct` nhưng không dùng để backfill `UnitPrice`, luôn lấy nguyên giá AI đọc được (thường là 0 vì khách không ghi giá bán trên đơn đặt hàng). Fix: `UnitPrice = matchedProduct?.UnitPrice ?? unitPrice`. Đồng thời sửa luôn product name matching quá cứng nhắc (thêm normalize khoảng trắng + fallback fuzzy contains 2 chiều, **có guard**: chỉ auto-match khi đúng 1 sản phẩm duy nhất thoả điều kiện mờ, tránh gán nhầm khi có ≥2 sản phẩm cùng tiền tố).
8. **Artifact User Guide** (claude.ai, KHÔNG nằm trong git repo) — cập nhật nhiều lần: thêm ví dụ cụ thể AI照合 (精密ギアA-100, 20個/3日以内 → risk 不足) vào demo scenario; thêm 2 câu hỏi mẫu cho AIチャット (bao gồm case "2 khách đặt cùng lúc" minh hoạ đúng logic double-counting đã fix); thêm section **06 導入準備** (dữ liệu khách cần chuẩn bị) và **07 料金目安** (3 mức chi phí AWS hosting: ミニマム/実用ミニマム/フル機能 + mục riêng chi phí Claude API, ước tính dựa trên `MaxTokens` đọc trực tiếp từ code 4 service AI); highlight card 受注登録 thành AI (đại diện AI照合, vì tính năng này không có menu riêng). **Link: https://claude.ai/code/artifact/d6ac3e4c-590e-4495-8b6c-2e7d39e42667** — lưu ý link này chỉ tồn tại trên claude.ai, không nằm trong git, phiên chat sau không tự biết trừ khi đọc devlog này.
9. **Tách repo mới `order-procurement-dx`** — clone từ `order-platform-dx` (giữ nguyên lịch sử commit sạch), làm điểm khởi đầu để xây module **仕入れ** (mua hàng/nhà cung cấp) — tính năng hoàn toàn CHƯA tồn tại trong hệ thống, chưa có thiết kế. Local: `f:\Prj\OrderProcurementDX`. Đã cài đặt sẵn: `npm install` (frontend), `dotnet restore`+build (backend, 0 lỗi).

## Quyết định quan trọng & lý do

- **User yêu cầu từ nay CHỈ làm việc trên `order-procurement-dx`**, không đụng `case-management` (`f:\Prj\CaseMngmt`) hay `order-platform-dx` (`f:\Prj\CaseMngmt-demo`) nữa — đã lưu vào memory system (`feedback_repo_scope.md`) để các phiên chat sau tự động biết.
- **Secret (Jwt:Secret, AWS ACCESS_KEY/SECRET_KEY, Anthropic ApiKey) lưu qua `dotnet user-secrets`**, KHÔNG ghi vào `appsettings.json` đang track git — vì `order-procurement-dx` cũng là repo **public**, ghi thẳng vào file tracked sẽ lặp lại đúng rủi ro rò rỉ secret đã gặp nhiều lần khi sync `order-platform-dx`. `UserSecretsId` trong `.csproj` (`2c30fbd9-1096-4927-8449-f0624f158a28`) giống hệt `case-management` (copy nguyên qua các lần robocopy sync trước đây) nên Anthropic key cũ trên máy tự dùng được ngay, chỉ cần set thêm Jwt:Secret + AWS keys.
- **Backend/frontend/ngrok cũ (chạy từ `case-management`) đã dừng hẳn** theo yêu cầu — hiện KHÔNG có demo nào đang chạy sống, không có ngrok URL nào active. Khi cần demo lại phải build+chạy từ `order-procurement-dx`.

## Vấn đề còn tồn đọng / blocked

- **Module 仕入れ (procurement/supplier) chưa có thiết kế gì cả** — cần 1 phiên plan riêng để chốt scope (Supplier entity? PurchaseOrder? liên kết thế nào với `Product.StockQuantity` hiện có — nhập hàng có cộng ngược vào tồn kho không?).
- **AWS key trong user-secrets của repo mới là key thật đang dùng CHUNG với `case-management` production** (bucket `case-bucket-ap-northeast`) — chưa tách tài nguyên AWS riêng cho project mới. Nếu module 仕入れ sau này cần dùng S3, nên cân nhắc tạo bucket/IAM riêng thay vì dùng chung key production.
- Các "Next Steps" cũ trong CLAUDE.md liên quan `case-management` gốc (rotate AWS key, thêm `*.log` vào `.gitignore` gốc, quyết định RAG...) — không còn thuộc phạm vi làm việc vì không đụng `case-management` nữa, nhưng vẫn là vấn đề thật tồn tại ở repo đó nếu sau này quay lại.

## Next steps (ưu tiên) — ĐÃ SUPERSEDED, xem phiên tiếp theo bên dưới

1. ~~Thiết kế module 仕入れ (procurement) — cần 1 buổi plan riêng, chưa bắt đầu.~~ → User yêu cầu đổi hướng: làm nền tảng dynamic-field trước (xem phiên tiếp theo).
2. Khi cần demo lại: build+deploy từ `order-procurement-dx` (`f:\Prj\OrderProcurementDX`), dựng lại ngrok tunnel mới (URL cũ `unlatch-crucial-tributary.ngrok-free.dev` gắn với process đã dừng, cần kiểm tra lại còn giữ được domain đó không khi khởi động ngrok mới).
3. Cân nhắc tách tài nguyên AWS riêng (S3 bucket/IAM) cho `order-procurement-dx` thay vì dùng chung key production của `case-management`.

---

## Phiên tiếp theo (cùng ngày) — Tổng quát hoá Dynamic Field (Template/Keyword) cho Product/Order

### Bối cảnh

User dừng việc thiết kế module 仕入れ lại (đã có 1 plan chi tiết cho 仕入れ chốt xong nhưng KHÔNG thực thi — bị huỷ giữa chừng khi user yêu cầu đổi hướng ngay trước `ExitPlanMode`). Lý do: 商品管理/注文管理 hiện là schema cố định, muốn thêm field mới bắt buộc phải sửa code — user muốn khách hàng tự thêm/bớt field được như màn hình 案件管理 (vốn đã có cơ chế Template/Keyword/Type dạng EAV), và muốn module 仕入れ tương lai dùng chung cơ chế này ngay từ đầu thay vì lại làm schema cố định.

### Đã làm

1. **Khảo sát sâu cơ chế EAV của Case** (`Template`/`Keyword`/`Type`/`CaseKeyword`) qua 2 Explore agent song song — phát hiện: `CaseKeyword{CaseId, KeywordId, Value:string}` đã có hình dạng khá tổng quát, nhưng Repository JOIN cứng vào `_context.Case`, và `CompanyTemplate` giả định 1 công ty = 1 template (code `.FirstOrDefault()` không filter). Đồng thời phát hiện Product/Order có field lõi (`StockQuantity`, `UnitPrice`, `ProductionCapacityPerDay`, `Quantity`...) đang bị đọc/ghi trực tiếp bằng property C# trong logic nghiệp vụ thật (AI照合, trừ/cộng kho, PDF) — khác bản chất Case nên **không thể** biến toàn bộ thành EAV.
2. **Backend — engine dynamic-field mới, hybrid**:
   - `Template.ModuleType` (mới, default `"Case"` — không đổi hành vi Case hiện có) + migration `AddTemplateModuleType`.
   - Bảng mới `EntityKeyword{EntityType, EntityId, KeywordId, Value}` (`backend/CaseMngmt.Model|Repository|Service/EntityKeywords/`) — dùng chung cho Product/Order/仕入れ sau này, **không đụng** bảng/dữ liệu `CaseKeyword` hiện có (Case giữ nguyên, zero-touch data layer). Migration `AddEntityKeywordTable`.
   - `TemplateService.EnsureModuleTemplateAsync`/`GetModuleTemplateAsync` + endpoint mới `GET /api/template/module?moduleType=` (`TemplateController.cs`) — tự tạo template rỗng cho company nếu chưa có (self-healing, không cần data migration backfill cho company cũ).
   - `CompanyController.Create` tự tạo Template rỗng cho Product/Order khi tạo company mới (song song với clone Standard Template cho Case như cũ).
   - `ProductRequest`/`OrderRequest`/`ProductViewModel`/`OrderViewModel` thêm `CustomFieldValues`; `ProductService`/`OrderService` wire vào Create/Update/GetById.
   - `KeywordService.SoftDeleteAsync` guard tổng quát hoá: route theo `Keyword.Template.ModuleType` (Case → check `CaseKeyword`, Product/Order → check `EntityKeyword`), vẫn trả 409 đúng convention cũ.
3. **Frontend**:
   - `GenericItems.js` (`frontend/src/components/until/`) thêm case `"textarea"` bị thiếu từ trước (bug cũ — field textarea trong Case im lặng không render gì, giờ render đúng).
   - `CustomFieldsSection.js` (mới) — component dùng chung, tái sử dụng `GenericItems` để render list field động.
   - `ProductDetail.js`/`OrderDetail.js` thêm section "カスタム項目" (load field def qua `templateService.getModuleTemplate`, gộp `customFieldValues` vào payload lưu). Order: custom field chỉ ở cấp header, không áp cho từng `OrderItem`.
   - `TemplateList.jsx` thêm cột "種別" (hiện Case/商品管理/受注管理) — Form Builder (`KeywordBuilder.jsx`) dùng lại nguyên vẹn, không cần route mới.
4. **Verify thật (không chỉ build)**: cài `dotnet-ef` global tool (pin version `6.0.25` khớp EF Core của project — bản `10.0.10` mặc định bị lỗi `FileLoadException` do version mismatch runtime) để xác nhận cách viết migration tay đúng convention của repo — phát hiện: migration viết tay CẦN có file `.Designer.cs` đi kèm mang attribute `[Migration("id")]` (nếu thiếu, `db.Database.Migrate()` âm thầm coi là "đã up to date", không báo lỗi, rồi crash runtime khi query cột mới `Invalid column name`). `BuildTargetModel` trong Designer.cs để RỖNG là đủ (đúng pattern đã có sẵn trong các migration cũ của repo như `AddIsHiddenForUser.Designer.cs`) — không cần snapshot đầy đủ. Đã chạy `dotnet run` thật với LocalDB (`MSSQLLocalDB`), xác nhận 2 migration mới áp dụng sạch, seed data chạy không lỗi, server listen bình thường.

### Quyết định quan trọng & lý do

- **Hybrid, không full-EAV**: giữ nguyên field lõi (có logic nghiệp vụ phụ thuộc: `StockQuantity`, `UnitPrice`, `ProductionCapacityPerDay`, `Quantity`, `CustomerId`, `OrderDate`...) là cột C# cố định; chỉ field bổ sung (không ảnh hưởng tính toán) mới cho dynamic qua `EntityKeyword`. Lý do: biến toàn bộ thành EAV đòi hỏi viết lại AI照合/trừ kho/PDF để đọc giá trị qua tra cứu KeywordId thay vì property C#, rủi ro cao và mất type-safety cho phép tính số học quan trọng — user tự chọn phương án này khi được hỏi.
- **`EntityKeyword` là bảng MỚI, không migrate `CaseKeyword` cũ vào chung**: giảm tối đa rủi ro cho Case đang chạy — chỉ sửa phần resolution Template/CompanyTemplate (metadata-level) để filter theo `ModuleType`, không đụng data layer của Case.
- **Không cần "Standard Template" clone cho Product/Order**: khác Case (có sẵn field mẫu để clone), Product/Order bắt đầu rỗng — company mới chỉ cần 1 Template trống để admin có chỗ thêm field, tạo lazy qua `EnsureModuleTemplateAsync` (cũng tự chữa cho company cũ tạo trước khi có tính năng này).
- **`GenericItems.js` hoá ra đã khá tổng quát sẵn** (không có hack "取引先名"/customerlist hay layout 2-cột cứng bên trong nó — các chỗ đó nằm ở `CaseDetail.js`/`CaseSearch.js` bao ngoài) — nên plan ban đầu "tách GenericItems thành component dùng chung" hoá ra chỉ cần thêm 1 case còn thiếu, KHÔNG cần sửa `CaseDetail.js`/`CaseSearch.js` — giảm rủi ro regression cho Case xuống gần như 0 (không đổi file nào của Case cả).
- **Dùng `dotnet ef migrations add` để sinh Designer.cs thất bại** vì snapshot hiện có của repo có 1 bug tiềm ẩn không liên quan (`Order.OrderItems` navigation lỗi khi tooling load snapshot cũ, do trước đây migration toàn viết tay không qua `dotnet ef`) — quyết định KHÔNG sửa bug lịch sử đó (ngoài phạm vi), thay vào đó tự viết Designer.cs tối giản theo đúng pattern rỗng đã có sẵn trong repo.

### Vấn đề còn tồn đọng / blocked

- Không test được full luồng UI qua trình duyệt thật (login → thêm field → điền form → lưu → reload) vì môi trường chạy lệnh hiện tại thiếu `Jwt:Secret` trong `dotnet user-secrets` (lỗi `IDX10703: key length is zero` khi gọi `/api/Account/login`) — đây là gap môi trường có sẵn từ trước (đã ghi trong CLAUDE.md phần "Không có demo nào đang chạy sống"), không phải lỗi do thay đổi lần này. Migration + seed + server start đã xác nhận sạch qua log thật.
- Search/filter theo giá trị custom field trên `ProductSearch.js`/`OrderSearch.js` — cố ý để ngoài phạm vi, chưa làm.
- Custom field ở cấp `OrderItem` (dòng chi tiết đơn hàng) — cố ý để ngoài phạm vi, chỉ làm ở cấp Order header.
- Thiết kế chi tiết entity module 仕入れ (Supplier/PurchaseOrder/GoodsReceipt/PurchaseInvoice) — plan cũ đã chốt xong nội dung (Supplier với 締め日/支払サイト kiểu Nhật, PurchaseOrder→GoodsReceipt cộng ngược `StockQuantity` đối xứng với Invoice trừ kho, AI đọc 見積書/納品書) nhưng **chưa thực thi** — cần làm lại/tái sử dụng plan đó, giờ sẽ dùng engine `EntityKeyword`/`ModuleType` mới này ngay từ đầu cho Supplier/PurchaseOrder thay vì lại làm schema cố định.
- `dotnet-ef` global tool giờ đã cài trên máy (pin `6.0.25`) — CLAUDE.md phần "Migration" đang ghi "dotnet CLI không có trên máy — tạo migration file thủ công" cần cập nhật lại (dotnet CLI có, migration tay vẫn là cách chính nhưng giờ biết chính xác cần Designer.cs kèm `[Migration]` attribute, không cần tool để verify nữa).

### Next steps (ưu tiên, cập nhật cuối phiên — thay thế mục cũ ở trên)

1. **Thiết kế + code module 仕入れ** (Supplier/PurchaseOrder/GoodsReceipt/PurchaseInvoice) dùng engine `EntityKeyword`/`ModuleType` mới — tái sử dụng nội dung plan đã chốt trong phiên trước (chưa lưu lại file, cần dựng lại từ đầu vì phiên chat sau không tự nhớ chi tiết plan cũ, chỉ nhớ khung: Supplier có 締め日+支払サイト+支払日, PurchaseOrder→GoodsReceipt cộng `StockQuantity` đối xứng InvoiceService trừ kho, AI đọc 見積書/納品書 theo pattern `OrderIntakeUpload`).
2. Search/filter theo custom field trên Product/Order search screens (fast-follow đã ghi nhận, chưa ưu tiên).
3. Khi cần demo lại: build+deploy, dựng lại ngrok tunnel mới (xem mục cũ phía trên — vẫn còn giá trị, chưa đổi).
4. Cân nhắc tách tài nguyên AWS riêng cho `order-procurement-dx` (vẫn còn giá trị, chưa đổi).

---

## Phiên tiếp theo (cùng ngày) — Test dynamic-field qua UI thật (Playwright headless)

### Đã làm

1. **Dựng môi trường test UI thật**: chạy backend (`ASPNETCORE_ENVIRONMENT=Development dotnet run --no-launch-profile`, cần `Development` để `dotnet user-secrets` load — chạy `--no-launch-profile` không có biến này mặc định là `Production`, đây chính là lý do lần chạy thử trước bị lỗi `Jwt:Secret` rỗng) + frontend (`npm start`, `REACT_APP_BASE_URL=http://localhost:5000` trỏ vào backend local, mặc định code trỏ prod `13.114.116.252`). Không có Playwright/chromium-cli cài sẵn và không có mạng để tải Chromium — dùng `playwright-core` (nhẹ, không kèm browser) + Microsoft Edge có sẵn trên máy (`channel: msedge` qua `executablePath`) làm driver headless, viết REPL driver riêng (`scratchpad/driver/driver.mjs`, dựa theo pattern trong skill `run`).
2. **Test click-through đầy đủ qua browser thật**: login SuperAdmin → mở 商品・在庫管理 và 受注登録 lần đầu (kích hoạt tự tạo Template rỗng qua `EnsureModuleTemplateAsync`) → vào テンプレート管理 xác nhận cả 3 template hiện đúng (BOAT Template/案件管理/21 field cũ, Product Template/商品管理/0 field mới, Order Template/受注管理/0 field mới, đều tạo lúc 2026/07/31) → dùng Form Builder thêm field "保管場所" (Alphanumeric) cho Product và "配送方法" (Alphanumeric) cho Order → tạo 1 Product mới và 1 Order mới có điền field custom → **reload từ đầu (browser mới, GET lại từ server) xác nhận cả 2 giá trị custom field persist đúng**.
3. **Phát hiện + fix 1 bug thật (không phải lỗi do phiên trước, do test lần này lộ ra)**: khi tạo field mới qua Form Builder mà để trống "最大文字数" (maxLength), `KeywordBuilder.jsx` gửi `maxLength: 0` lên backend (`formData.maxLength ? parseInt(...) : 0`) thay vì `null`. `0` được lưu là `Keyword.MaxLength = 0`, và `GenericItems.js` (case `"string"`/`"textarea"`) truyền thẳng `maxLength={props.maxLength}` xuống `<input>` native — HTML `maxlength="0"` khiến input **không nhận được bất kỳ ký tự nào** (typing bị chặn hoàn toàn ở tầng browser, không phải lỗi React). Đây là bug **có sẵn từ trước** trong `KeywordBuilder.jsx`, chỉ chưa ai gặp vì Case luôn set maxLength khi tạo field qua UI cũ. Fix: `frontend/src/components/until/GenericItems.js` — đổi `maxLength={props.maxLength}` → `maxLength={props.maxLength || undefined}` ở cả 2 case `"string"` và `"textarea"` (dùng `replace_all`), omit attribute hoàn toàn khi maxLength falsy/0 thay vì set 0. Không sửa `KeywordBuilder.jsx` (root cause thật ở đó) vì fix ở renderer đã đủ chặn triệt để và an toàn hơn (không đổi payload gửi lên backend, không rủi ro cho Case).

### Quyết định quan trọng & lý do

- **Không có tool chạy app sẵn (`chromium-cli` không tồn tại trong môi trường này, không có mạng tải Chromium)** — tự viết driver Node.js dùng `playwright-core` (không kèm browser binary, cài nhanh) trỏ vào Edge có sẵn trên máy Windows, theo đúng pattern REPL của skill `run` (`examples/electron.md` là gốc, chuyển từ `_electron` sang `chromium.launch` thường vì đây là web app, không phải Electron).
- **Không dùng `getByLabel`** cho các field tự code (`FormInput`/`.section-item` pattern) — component này không có `htmlFor`/`id` liên kết label↔input chuẩn HTML, nên Playwright's `getByLabel` không hoạt động. Chỉ dùng được cho MUI TextField/Select thật (trong `KeywordBuilder.jsx`). Với `.section-item`, phải tự viết `fill-near`/`click-near` (tìm `.section-item` chứa text label rồi lấy input con) — và **phải scope trong `.MuiDialog-root`** khi form hiện dưới dạng Dialog, vì trang nền (`ProductSearch.js`) vẫn còn mounted phía sau và có field trùng tên ("品名") gây fill nhầm.
- **Nút "追加"/"保存" trong dialog dễ bị match nhầm với nút cha bên ngoài** (vd `+ フィールド追加` chứa substring "追加") khi dùng text-substring locator — phải dùng `getByRole('button', {name, exact: true})` để tránh click nhầm (từng khiến việc "lưu field mới" tưởng thành công nhưng thực ra không hề gọi API).
- **`wait-text` không match được giá trị bên trong `<input>`** (Playwright's text engine chỉ scan text node, không đọc `.value` của input) — verify giá trị input phải dùng `eval ... .value`, không dùng `wait-text`. Một lần tưởng nhầm là "Order không persist custom field" hoá ra chỉ là race trong chính test script (đọc DOM trước khi `getOrderDetail()` fetch xong) — đã xác nhận lại bằng cách gọi thẳng API (`curl GET /api/order?id=`) thấy dữ liệu đã lưu đúng từ đầu, và bằng cách wait đúng giá trị thay vì chỉ wait label.

### Vấn đề còn tồn đọng / blocked

- Không có gì blocked — dynamic-field cho Product/Order đã verify hoạt động đúng end-to-end qua UI thật (không chỉ build/migration như phiên trước).
- Dữ liệu test còn sót lại trong LocalDB (`MSSQLLocalDB`, database `CaseMngmt`) sau phiên test: 1 Product "テスト部品ABC", 2 Order (`ORD-2026-00024` tạo qua UI, `ORD-2026-00025` tạo qua API curl để debug) — không dọn vì đây là DB dev cục bộ, không phải production, không ảnh hưởng gì nếu để lại; xoá thủ công qua UI nếu muốn dọn sạch.
- `.claude` scratchpad driver (`driver.mjs`) chỉ tồn tại trong thư mục scratchpad phiên này (`C:\Users\hoang\AppData\Local\Temp\claude\...\scratchpad\driver\`), KHÔNG lưu vào repo — phiên sau muốn test UI lại qua browser headless sẽ phải viết lại driver này từ đầu (không có project skill `run` riêng cho repo này). Cân nhắc dùng `/run-skill-generator` nếu việc test UI qua browser headless trở thành nhu cầu thường xuyên.

### Next steps (cập nhật, ưu tiên)

1. Thiết kế + code module 仕入れ — vẫn là ưu tiên #1 như đã ghi ở trên, chưa đổi.
2. Cân nhắc chạy `/run-skill-generator` để lưu lại driver test-UI-qua-browser thành project skill chính thức (`playwright-core` + Edge headless) nếu sẽ còn cần test UI thật nhiều lần nữa — hiện driver chỉ nằm ở scratchpad tạm, mất khi hết phiên.
3. Search/filter theo custom field, module 仕入れ, tách AWS — như đã ghi ở trên, chưa đổi.
