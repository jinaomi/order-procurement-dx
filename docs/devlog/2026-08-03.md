# 2026-08-03 — Redesign toàn bộ giao diện (theme màu mới + điều hướng mới)

## Đã làm

Thực hiện theo plan đã duyệt (`~/.claude/plans/anh-mu-n-thay-i-vectorized-seal.md`), toàn bộ trên branch riêng **`feature/ui-redesign`** (tạo từ `main`, theo yêu cầu cách ly công việc redesign khỏi `main`) — **chưa commit**, xem mục "Vấn đề còn tồn đọng" bên dưới.

### Nền tảng theme
- `frontend/src/theme.js` (mới): theme MUI trung tâm đầu tiên của app — palette navy `#1F3A5F` (primary) + copper `#B85A25` (secondary, tái dùng đúng màu `AI_ACCENT_COLOR` cũ), success/warning/error/info lấy đúng giá trị đã hardcode sẵn trong `SalesDashboard.js` (để không đổi ý nghĩa màu trạng thái quen mắt). Override `MuiTableHead`/`MuiTableCell`/`MuiTableRow`/`MuiButton`/`MuiAppBar`/`MuiDrawer`... để cascade tự động ra toàn app.
- Mount `<ThemeProvider>`+`<CssBaseline/>` ở **`frontend/src/index.js`** (gốc thật, không phải `App.js`) — để Login/Register/admin routes cũng có theme (trước đây chỉ Sidebar có theme cục bộ, chỉ chỉnh typography).
- `frontend/src/index.css`: xoá tận gốc `html{font-size:24px}` (thay vì tiếp tục vá `!important`), xoá hack ép toàn bộ `<Table>` thành nền teal, xoá hardcode xanh lá `.search-close/.search-edit/.search-delete`.

### Điều hướng mới (`Sidebar.js`)
- Giữ nguyên 100% cơ chế `header`-state điều hướng giả (không phải React Router thật) — không đụng vào bất kỳ trang nghiệp vụ nào trong 19 trang.
- Bỏ resize-by-drag thủ công, thay bằng **mini-variant collapse chuẩn MUI** (260px/64px, nhớ trạng thái qua `localStorage`).
- Nhóm lại menu theo miền nghiệp vụ: マスタ管理 (mới, gồm 顧客/商品/仕入先 — trước đây 仕入先 nằm trong 仕入れ管理), 受注管理, 仕入れ管理, レポート・ツール (gộp 経営ダッシュボード/書類管理/AIチャット).
- Branding "受注・仕入 業務管理システム" lên đầu Drawer; logout chuyển từ Drawer sang menu Avatar ở AppBar.
- Sửa `mt:"140px"` (magic number) bằng `<Toolbar/>` spacer chuẩn.

### Component dùng chung
- `FormButton.js`: giữ nguyên prop API (`buttonType`), đổi bên trong từ hex cứng sang MUI `color`/`variant` — sửa luôn 1 bug thật: `delete` và `attach` trước đây bị gộp chung 1 màu xanh dương, giờ `delete`→`error` (đỏ) đúng ngữ nghĩa.
- `Login.js`: viết lại hoàn toàn từ `<input>`/`<label>` thô sang MUI `Card`/`TextField`/`Alert`. Đã xử lý đúng gotcha `inputRef` (không phải `ref`) cho input auto-focus, tránh crash. Bỏ dòng "Version 24.1.1000 Powered by ITFreee" theo yêu cầu.
- Dọn hex cứng còn sót ở 7 file: `ContentDialog.js`, `CaseDetail.js`, `document-management/DocumentSearch.js`, `until/AttachedFilesList.js`, `until/DialogHandle.js`, `ChatAssistant.js`, `SalesDashboard.js`.
- Tab title đổi thành "受注・仕入 業務管理システム", favicon đổi từ icon cũ rối (5 hình ghép, `resource.png`) sang icon mới đơn giản (`favicon.svg`, 2 mũi tên ngược chiều tượng trưng luồng 受注/仕入れ, nền navy đồng bộ theme).

### 2 bug phát hiện qua QA thật trong phiên (ngoài phạm vi plan gốc, đã sửa)
1. **15 file `*Search.js`** hardcode `color="success"` cho CẢ nút 編集 lẫn 削除 (cả 2 cùng xanh lá, kể cả xoá) — phát hiện khi chụp ảnh bảng `PurchaseOrderSearch` có data thật. Đã sửa: 編集/表示/ダウンロード → `primary`, 削除 → `error`, giữ nguyên các nút mang ý nghĩa hoàn tất tích cực thật (vd "入金済みにする") là `success`.
2. **Sidebar cuộn dở rồi mở group khác** → menu mới bị cắt mất phần trên do vị trí cuộn không tự điều chỉnh. Sửa bằng `scrollIntoView({block:"start"})` khi mở 1 group.
3. **Chuyển trang (đổi `header`) không reset scroll cửa sổ chính** → trang mới hiện ra bị cắt nếu trang trước đã cuộn xuống. Sửa bằng `useEffect(() => window.scrollTo(0,0), [header])`.

### Kiểm thử
Playwright + Edge (`channel:"msedge"`, cài tạm trong scratchpad ngoài repo, KHÔNG thêm vào `package.json`) — verify qua browser thật: chrome AppBar/Drawer, cả 4 nhóm menu, collapse/expand, user avatar menu, bảng dữ liệu thật (発注検索 với PO-2026-*), Chip trạng thái, `ConfirmBox` (nút "はい" đỏ), dialog フィールド追加 trong テンプレート管理 (nơi từng có bug maxLength/font-size — xác nhận KHÔNG tái phát), và 2 bug scroll ở trên sau khi sửa.

`npm run build` (production) chạy sạch, không lỗi mới, chỉ warning ESLint cũ có sẵn từ trước phiên này. Bundle tăng nhẹ +3.5kB gzip.

## Quyết định quan trọng & lý do

- **Không migrate `Sidebar.js` sang React Router thật** dù đang redesign toàn bộ điều hướng — vì 19 trang nghiệp vụ phụ thuộc cơ chế `header`-state hiện tại, đổi sang route thật sẽ kéo theo sửa toàn bộ trang, ngoài phạm vi "redesign giao diện". Đã ghi rõ trong plan làm ràng buộc bắt buộc.
- **Chọn success/warning/error/info trùng giá trị hex cũ đã dùng trong `SalesDashboard.js`** thay vì chọn màu mới hoàn toàn — để Chip trạng thái không đổi ý nghĩa màu người dùng đã quen, giảm rủi ro nhầm lẫn khi demo.
- **Mount theme ở `index.js` chứ không phải `App.js`** — vì Login/Register/admin routes nằm ngoài `ThemeProvider` cục bộ cũ của Sidebar, cần theme ở gốc thật để áp dụng đều cho mọi route.
- **Không đổi `Register.js`/`Home.js`** — hai file này vẫn dùng class CSS thô (`.select-form`, `.errmsg`, `.offscreen`) nên các class này được GIỮ LẠI trong `index.css` dù `Login.js` không còn dùng nữa. Ngoài phạm vi phiên này (không nằm trong plan đã duyệt); nếu redesign tiếp Register/Home sau này, các class thô này mới có thể dọn hẳn.
- **Không chạy `dotnet build`** trong phiên này — backend không bị đụng tới (chỉ sửa frontend), và backend đang chạy nền thật (phục vụ demo cũ trên `main`) nên build sẽ bị lock file (MSB3027) nếu chạy song song.

## Vấn đề còn tồn đọng / blocked

- **Toàn bộ thay đổi CHƯA COMMIT** — nằm trên branch `feature/ui-redesign`, working tree có thay đổi tại ~26 file (xem danh sách "File chính sẽ sửa" trong plan). User đang xem qua trước khi quyết định commit + có merge vào `main` hay không.
- Dev server frontend (`npm start`, port 3001) có thể vẫn đang chạy nền từ phiên test — cần dừng nếu không dùng nữa (xem mục Dọn dẹp).
- Playwright + Edge được cài tạm trong thư mục scratchpad ngoài repo (`node_modules`/`package.json` riêng) — không ảnh hưởng repo, tự dọn khi scratchpad bị xoá.

## Next steps

1. **User review + quyết định commit/merge** `feature/ui-redesign` — việc cần làm ngay tiếp theo, chưa làm gì thêm cho tới khi có quyết định này.
2. Sau khi merge: cân nhắc build lại production (`npm run build` → mirror `wwwroot`) để cập nhật demo ngrok đang chạy sang giao diện mới (demo hiện tại trên `main` vẫn là giao diện teal cũ, không bị ảnh hưởng bởi các thay đổi trên branch).
3. Cân nhắc redesign nốt `Register.js`/`Home.js` nếu muốn dọn sạch hoàn toàn class CSS thô còn sót trong `index.css`.
4. Các next-step tồn đọng từ trước (không thuộc phạm vi phiên này, xem `CLAUDE.md`): search theo custom field trên Product/Order/Supplier Search, 3 vấn đề 書類管理 (phân trang/案件 đã đóng/thiếu InvoiceDetail.js), sequence table cho đánh số chứng từ, Excel import Product, 4 mảnh nghiệp vụ 仕入れ còn thiếu, tách AWS resource riêng, quyết định RAG.
5. **Đã hoàn thành trong phiên này, xoá khỏi Next Steps của CLAUDE.md**: "Cải thiện giao diện login" (mục 8 cũ) và "Đổi text 受注管理システム trong Sidebar" (mục 10 cũ) — cả 2 đã làm xong.

## Cập nhật (phiên sau, 2026-08-04)

User review qua browser thật, phản hồi vẫn còn rối/chưa chuyên nghiệp ở vài chỗ cụ thể — đã tinh chỉnh tiếp trên cùng branch `feature/ui-redesign` trước khi commit:

- **Header bảng nhẹ hơn**: `theme.js` — `MuiTableHead`/`MuiTableCell` đổi từ khối navy đặc + chữ trắng sang nền sáng + chữ navy đậm + `borderBottom` 2px thay viền đặc.
- **Tăng cỡ chữ toàn app**: `theme.js` — `typography.fontSize` 14→15 theo yêu cầu "dễ đọc hơn".
- **`FormSection.js` (component mới)**: Card `variant="outlined"` bọc nội dung form, có `title` tuỳ chọn (Typography đậm navy + Divider) — áp dụng cho 7 trang detail (`OrderDetail`/`PurchaseOrderDetail`/`GoodsReceiptDetail`/`PurchaseInvoiceDetail`/`SupplierDetail`/`ProductDetail`/`CustomerDetail`) để field không còn nằm trần trên nền xám. Riêng `PurchaseOrderDetail.js` (dialog bị chê rối nhất) tách thành 3 `FormSection` riêng: ステータス・対応状況 / 発注書発行・送付履歴 / 発注情報.
- **`FormButton.js`**: thêm `buttonType="secondaryAction"` (→ `color="primary" variant="outlined"`) cho hành động phụ (vd 新規作成), phân biệt với 保存 (`contained`, nổi bật hơn) — cùng với bỏ ép `width:"100%"` ở các nút này (`sx={{width:"auto"}}`) để hết cảnh 3 nút to bằng nhau xếp cạnh nhau.
- **Sidebar branding**: thêm icon mark nhỏ (motif 2 mũi tên từ `favicon.svg`, vẽ trực tiếp bằng `<svg>` inline) cạnh tên hệ thống, tăng khoảng cách với nút thu gọn — trước đó user chỉ ra khối 2 dòng chữ sát nút chevron nhìn cấn.
- **`CustomFieldsSection.js`**: đổi tiêu đề "カスタム項目" từ `Divider`+`Chip` (kiểu viên thuốc giữa đường kẻ, lạc tông trong Card mới) sang `Typography` đậm navy + `Divider` thường — đồng bộ với tiêu đề `FormSection`.

Tất cả đã test lại qua Playwright+Edge (chụp trước/sau từng bước) và `npm run build` sạch sau mỗi thay đổi.

**Quyết định giữ giao diện cũ làm đường lùi**: user muốn dùng giao diện mới làm chính nhưng cần khả năng quay lại nếu phát sinh bug, hoặc sau này khách hàng yêu cầu giao diện cũ. Đã tư vấn 3 hướng (branch/tag đóng băng; deploy song song 2 bản; toggle runtime trong app) — chọn hướng **branch đóng băng** (đơn giản nhất, không tốn bảo trì lâu dài, đúng nguyên tắc "không dùng feature-flag khi có thể đổi code thẳng" đã có sẵn trong `CLAUDE.md`):
- Branch **`ui-legacy`** tạo tại `main` trước khi merge (commit `a76394c`) — đóng băng vĩnh viễn giao diện cũ, không phát triển tiếp.
- Redesign (đã tinh chỉnh xong) được commit và merge vào `main` — `main` từ giờ là giao diện mới.
- Không xây cơ chế toggle runtime — lý do: phải duy trì song song 2 bộ code UI mãi mãi cho mọi thay đổi sau này, chi phí quá cao so với nhu cầu thực tế (chỉ cần đường lùi, không cần chạy đồng thời cho user chọn).
- Cách dựng lại giao diện cũ khi cần: `git checkout ui-legacy` → `npm run build` (frontend) → mirror `wwwroot` → deploy.

**Next steps cập nhật**: build lại production + cập nhật demo ngrok đang chạy sang giao diện mới vẫn còn treo (cần user xác nhận trước khi rebuild bản demo sống hiện tại, đang phục vụ giao diện cũ).
